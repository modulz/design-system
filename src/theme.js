import { createStyled } from '@stitches/styled';

export const { styled } = createStyled({
  tokens: {
    colors: {
      red100: "tomato",
      blue500: "blue",
      gray500: "gray",
      green100: "green",
      yellow100: "yellow",
    }
  }
});
